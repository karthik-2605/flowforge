name: Docker Build Check

on:
  push:
    branches: [main]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image
        run: docker build ./backend -t flowforge-backend

      - name: Build frontend image
        run: docker build ./frontend -t flowforge-frontend
